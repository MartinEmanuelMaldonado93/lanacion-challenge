// import { renderHook } from "@testing-library/react";
// import { useFilteredTags } from "./hooks";

describe("useFilteredTags hook", () => {
  it("should return 10 elements", () => {
    // const { result } = renderHook(() => useFilteredTags());
    // expect(result.current.data.length).toBe(10);
  });
});
